import { connect } from 'react-redux';
import Container from './container';

// Add all the action for: LogIn, SginUp, Recover Password, Check username, Check Password, Check Email

export default connect()(Container);